#include <bits/stdc++.h>

using namespace std;

int n,sum = 0;

void start(void)
{
	cin>>n;
	sum = 0;
}

int tong(int n)
{
	if (n==0)
		return 0;
	else
		return n + tong(n-1);
}

int main()
{
	int t;
	cin>>t;
	while (t--)
	{
		start();
		cout<<tong(n);
	}
}
