#include <bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin>>t;
	while (t--){
		int n;
		cin>>n;
		queue <string> q;
		q.push("9");
		while (!q.empty()){
			int num = stoi(q.front());
			q.pop();
			if (num % n == 0){
				cout<<num<<endl;
				break;
			}else{
				q.push(to_string(num) + "0");
				q.push(to_string(num) + "9");
			}
		}
	}
	
	return 0;
}
