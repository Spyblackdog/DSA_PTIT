#include <bits/stdc++.h>

using namespace std;

int n,x,a[1005],b[1005];

int check(int k){
	int sum = 0;
	for (int i=1;i<=k;i++){
		sum += a[b[i]];
	}
	
	return sum;
}

void in(int k){
	cout<<"[";
	for (int i=1;i<=k;i++){
		cout<<a[b[i]]<<" ";
	}
	cout<<"]";
	cout<<" ";
}

void Try(int i,int k){
	for (int j = 1; j <= k; j++) {
			b[i] = j;
			if (i == k) {
				if (check(k) == x)
					in(k);
			} else
				Try(i + 1,k);
	}
}

int main(){
	int t;
	cin>>t;
	while (t--){
		cin>>n>>x;
		for (int i=1;i<=n;i++)
			cin>>a[i];
		for (int k=1;k<=n;k++){
			Try(1,k);
		}
	}
	
	return 0;
}
