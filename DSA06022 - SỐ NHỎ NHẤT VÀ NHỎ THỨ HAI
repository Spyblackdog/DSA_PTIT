#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){
	int t;
	cin>>t;
	while (t--){
		int n;
		cin>>n;
		ll a[n];
		ll m1 = INT_MAX,m2 = INT_MAX;
		for (int i = 0;i<n;i++){
			cin>>a[i];
			if (m1 > a[i]){
				m2 = m1;
				m1 = a[i];
			}
			if (a[i] != m1 && m2 > a[i]){
				m2 = a[i];
			}
		}
		if (m2 == INT_MAX || m1 == INT_MAX)
			cout<<-1<<endl;
		else
			cout<<m1<<" "<<m2<<endl;
		
	}
	
	return 0;
}
