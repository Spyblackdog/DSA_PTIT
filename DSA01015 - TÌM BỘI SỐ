#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main(){
	ll t;
	cin>>t;
	while (t--){
		ll n;
		cin>>n;
		queue <ll> q;
		q.push(9);
		while (q.front() % n != 0){
			ll num = q.front();
			q.pop();
			q.push(num * 10);
			q.push((num * 10) + 9);
		}
		cout<<q.front()<<endl;
	}
	
	return 0;
}
