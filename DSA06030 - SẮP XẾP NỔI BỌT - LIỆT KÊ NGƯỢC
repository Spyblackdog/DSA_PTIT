#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin>>t;
    while (t--){
        int n;
        cin>>n;
        int a[n];
        int cnt = 1;
        vector <vector <int>> v;
        for (int i=0;i<n;i++)
            cin>>a[i];
        for (int i=0;i<n-1;i++){
            int k = 0;
            for (int j=0;j<n-i-1;j++){
                if (a[j] > a[j + 1]){
                    swap(a[j],a[j+1]);
                    k++;
                }
            }
            if (k != 0){
                vector <int> vec;
                for (int k=0;k<n;k++){
                    vec.push_back(a[k]);
                }
                v.push_back(vec);
                cnt++;
            }
        }
        for (int i = v.size()-1;i>=0;i--){
            cout<<"Buoc "<<i+1<<": ";
            for (int j=0;j<v[i].size();j++){
                cout<<v[i][j]<<" ";
            }
            cout<<endl;
        }
    }

    return 0;
}
