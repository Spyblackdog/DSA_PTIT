#include <bits/stdc++.h>

using namespace std;

int n,a[1005],dem = 0;
bool ok = true;

void start(void)
{
	cin>>n;
	for (int i=1;i<=n;i++)
		a[i] = i;
	ok = true;
}

void solve(void)
{
	int i=n-1;
	while (i>0 && a[i+1] < a[i])
	{
		i--;
	}
	if (i<=0)	
	{
		ok = false;
	}else{
		int k=n;
		while (k>i && a[i] > a[k])
			k--;
		swap(a[k],a[i]);
		int l=i+1;int r=n;
		while (l<r)
		{
			swap(a[l],a[r]);
			l++;r--;
		}
	}
}

void in(void)
{
	for (int i=1;i<=n;i++)
		cout<<a[i]<<" ";
}

int main()
{
	int t;
	cin>>t;
	while (t--)
	{	
		start();
		while (ok == true)
		{
			in();
			cout<<endl;
			solve();
		}
		cout<<"End Test!"<<endl;
	}
}
