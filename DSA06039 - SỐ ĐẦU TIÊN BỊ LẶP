#include <bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin>>t;
    while (t--){
        int n;
        cin>>n;
        int a[n];
        map <int,int> m,fi;   
        for (int i=0;i<n;i++){
            cin>>a[i];
            if (m[a[i]] == 0){
                fi[a[i]] = i;
            }
            m[a[i]]++;
        }
        int ans = -1;
        int minindex = INT_MAX;
        for (int i=0;i<n;i++){
            if (m[a[i]] > 1){
                if (minindex > fi[a[i]]){
                    minindex = fi[a[i]];
                    ans = a[i];
                }
            }
        }
        if (ans == -1)
            cout<<"NO"<<endl;
        else   
            cout<<ans<<endl;
    }

    return 0;
}
