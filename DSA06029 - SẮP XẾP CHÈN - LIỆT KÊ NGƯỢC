#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin>>n;
    vector <int> v(n);
    vector <vector<int>> vec;
    for (int i=0;i<n;i++){
        cin>>v[i];
    }
    for (int i=1;i<n;i++){
        vector <int> b;
        for (int k = 0;k<i;k++)
            b.push_back(v[k]);
        vec.push_back(b);
        int res = v[i];
        int j = i - 1;
        while (j >= 0 && v[j] > res)
            j--;
        v.erase(v.begin() + i);
        v.insert(v.begin() + (j+1),res);
    }
    vector <int> c;
    for (int i=0;i<n;i++){
        c.push_back(v[i]);
    }
    vec.push_back(c);
    int cnt = n-1;
    for (int i=vec.size()-1;i>=0;i--){
        cout<<"Buoc "<<cnt--<<": ";
        for (int j=0;j<vec[i].size();j++){
            cout<<vec[i][j]<<" ";
        }
        cout<<endl;
    }

    return 0;
}
