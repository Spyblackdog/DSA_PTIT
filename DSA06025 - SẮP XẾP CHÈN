#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin>>n;
    vector <int> v(n);
    for (int i=0;i<n;i++){
        cin>>v[i];
    }
    for (int i=1;i<n;i++){
        cout<<"Buoc "<<i-1<<": ";
        for (int k = 0;k<i;k++)
            cout<<v[k]<<" ";
        cout<<endl;
        int res = v[i];
        int j = i - 1;
        while (j >= 0 && v[j] > res)
            j--;
        v.erase(v.begin() + i);
        v.insert(v.begin() + (j+1),res);
    }
    cout<<"Buoc "<<n - 1<<": ";
    for (int i=0;i<n;i++)
        cout<<v[i]<<" ";

    return 0;
}
