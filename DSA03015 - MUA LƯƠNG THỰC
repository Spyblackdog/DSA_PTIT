#include <bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin>>t;
	while (t--){
		int n,m,s;
		cin>>n>>s>>m;
		int slmd = n*(s - (s/7));
		int slc = s*m;
		if (slmd >= slc){
			cout<<(slc + n - 1)/n<<endl;
		}else{
			cout<<-1<<endl;
		}
		
		
	}
	
	return 0;
}
