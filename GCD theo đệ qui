#include <bits/stdc++.h>

using namespace std;

int a,b;

void start(void)
{
	cin>>a>>b;
}

int gcd(int n,int m)
{
	if (m == 0)
		return n;
	else
	{
		int r = n % m;
		n = m;
		m = r;
		return gcd(n,m);
	}
}

int main()
{
	int t;
	cin>>t;
	while (t--)
	{
		start();
		cout<<gcd(a,b)<<endl;
	}
}
