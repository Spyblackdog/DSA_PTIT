#include <bits/stdc++.h>

using namespace std;

vector <int> v[10005];
bool ok[10005];

void dfs(int u){
	cout<<u<<" ";
	ok[u] = true;
	for (auto x : v[u]){
		if (!ok[x]){
			dfs(x);
		}
	}
}

int main(){
	int t;
	cin>>t;
	while (t--){
		int n,m,k;
		cin>>n>>m>>k;
		for (int i=1;i<=m;i++){
			int x,y;
			cin>>x>>y;
			v[x].push_back(y);
		}
		memset(ok,false,sizeof(ok));
		dfs(k);
		for (int i=1;i<=n;i++)
			v[i].clear();
		cout<<endl;
	}
	
	return 0;
}
