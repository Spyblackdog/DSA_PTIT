#include <bits/stdc++.h>

using namespace std;

vector <int> v[10005];
int color[10005];

bool dfs(int u){
	color[u] = 1;
	for (auto x : v[u]){
		if (color[x] == 0){
			if (dfs(x))
				return true;
		}else if (color[x] == 1){
			return true;
		}
	}
	color[u] = 2;
	return false;
}

int main(){
	int t;
	cin>>t;
	while (t--){
		int n,m;
		cin>>n>>m;
		for (int i=1;i<=m;i++){
			int x,y;
			cin>>x>>y;
			v[x].push_back(y);
		}
		memset(color,0,sizeof(color));
		bool ok = false;
		for (int i=1;i<=n;i++){
			if (color[i] == 0){
				if (dfs(i) == true){
					ok = true;
					break;
				}
			}
		}
		if (ok)
			cout<<"YES";
		else
			cout<<"NO";
		cout<<endl;
		for (int i=1;i<=n;i++)
			v[i].clear();
	}
	
	return 0;
}
