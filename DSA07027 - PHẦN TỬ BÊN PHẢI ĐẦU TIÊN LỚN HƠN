#include <bits/stdc++.h>

using namespace std;

struct node{
	int data;
	node* next;	
};

node *makenode(int x){
	node *newnode = new node();
	newnode -> data = x;
	newnode -> next = NULL;
	
	return newnode;
}

void push(node* &head,int x){
	node *newnode = makenode(x);
	if (head == NULL){
		head = newnode;
	}else{
		newnode -> next = head;
		head = newnode;
	}
}

void pop(node* &head){
	if (head == NULL)
		return;
	if (head -> next == NULL){
		delete head;
		head = NULL;
		return;
	}
	node *temp = head;
	head = head -> next;
	delete temp;
}

bool check(node *head){
	return head == NULL;
}

int main(){
	int t;
	cin>>t;
	while (t--){
		node *head = NULL;
		int n;
		cin>>n;
		int a[n],b[n];
		for (int i=0;i<n;i++){
			cin>>a[i];
			b[i] = -1;
		}
		for (int i=n-1;i>=0;i--){
			while (!check(head) && head -> data <= a[i]){
				pop(head);
			}
			if (!check(head)){
				b[i] = head -> data;
			}
			push(head,a[i]);
		}
		for (int i=0;i<n;i++){
			cout<<b[i]<<" ";
		}
		cout<<endl;
	}
	
	return 0;
}
