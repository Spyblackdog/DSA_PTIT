#include <bits/stdc++.h>

using namespace std;

int main(){
	int t;
	cin>>t;
	while (t--){
		int n,m,l;
		cin>>n>>m>>l;
		int a[n],b[m],c[l];
		for (int i=0;i<n;i++)	cin>>a[i];
		for (int i=0;i<m;i++)	cin>>b[i];
		for (int i=0;i<l;i++)	cin>>c[i];
		int i=0,j=0,k=0;
		bool ok = false;
		while (i < n && j < m && k < l){
			if (a[i] == b[j] && b[j] == c[k]){
				cout<<a[i]<<" ";
				ok = true;
				i++;
				j++;
				k++;
			}else if (a[i] < b[j]){
				i++;
			}else if (b[j] < c[k]){
				j++;
			}else{
				k++;
			}
		}
		if (ok == false)
			cout<<"NO";
		cout<<endl;
	}
	
	
	return 0;
}
